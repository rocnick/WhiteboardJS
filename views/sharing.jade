extends layout

block content
  div(id='sharingContainer')
    p Board Sharing
    div(class='boardShare' ng-repeat='x in boardInfo')
      svg(class='boardThumb' data-polygons='{{x.Polygons}}' data-boardid='{{x.BoardID}}')
      select
        option(ng-repeat='y in users' value='{{y.UserID}}') {{y.Username}}
